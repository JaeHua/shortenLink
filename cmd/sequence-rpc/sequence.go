package main

import (
	"flag"
	"fmt"

	"shortenLink/cmd/sequence-rpc/internal/config"
	"shortenLink/cmd/sequence-rpc/internal/server"
	"shortenLink/cmd/sequence-rpc/internal/svc"
	"shortenLink/cmd/sequence-rpc/shortenLink/rpc/sequence"

	"github.com/zeromicro/go-zero/core/conf"
	"github.com/zeromicro/go-zero/core/service"
	"github.com/zeromicro/go-zero/zrpc"
	"google.golang.org/grpc"
	"google.golang.org/grpc/reflection"
)

var configFile = flag.String("f", "etc/sequence.yaml", "the config file")

func main() {
	flag.Parse()

	var c config.Config
	conf.MustLoad(*configFile, &c)
	ctx := svc.NewServiceContext(c)

	s := zrpc.MustNewServer(c.RpcServerConf, func(grpcServer *grpc.Server) {
		sequence.RegisterSequenceServer(grpcServer, server.NewSequenceServer(ctx))

		if c.Mode == service.DevMode || c.Mode == service.TestMode {
			reflection.Register(grpcServer)
		}
	})
	defer s.Stop()

	fmt.Printf("Starting rpc server at %s...\n", c.ListenOn)
	s.Start()
}
